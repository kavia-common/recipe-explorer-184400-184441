# Recipe Explorer

A Recipe app where users can browse, search, and view recipes with details such as ingredients, instructions, and images.

Container:
- frontend_app (Next.js) – UI with local mock APIs, ready to connect to a backend.

Run frontend:
1) cd frontend_app
2) npm install
3) npm run dev